# Phase 0 Audit — Foundation and migrated source

## Scope completed

- Created the canonical `apps/api` Python application boundary.
- Copied the extracted backend source and tests into `apps/api` for migration.
- Added `apps/api/PLAN.md` with phase ownership, dependencies, acceptance gates and final audit rules.
- Added typed settings, package metadata, local documentation and a non-root Dockerfile.
- Added the first canonical schemas, repositories, domain primitives, synthetic runner, orchestration service, REST routes and WebSocket route.
- Kept `app/trio` and `app/validation` as explicitly labelled migration references.

## Verification

- `python -m compileall apps/api/app`: passed.
- Synthetic TestClient vertical slice: passed.
- `/health`: 200.
- `/v1/datasets`: 200.
- `POST /v1/runs`: 201.
- `POST /v1/runs/{id}/start`: 200.
- Snapshot novelty: offspring 31.0 outside parental range 12.0–18.0.
- Trace: 13 candidates; top candidate `E_L10_L31`.
- Counterfactual: `break_interaction` returns a model-relative delta.
- Timeline: 26 ordered events ending in `run_completed`.

## Current limitations

- Repository is in-memory only.
- Real-trio mode is contract-visible but intentionally gated until its manifest and adapter are migrated.
- The copied legacy tests still depend on data files not present in the source archive.
- WebSocket replay uses bounded repository polling for the single-process MVP; distributed fan-out is deferred.

## Phase scorecard

1. Code Quality: 6.5/10 — canonical code is separated, but migration references and new code coexist.
2. Code Readability: 7.0/10 — typed schemas and focused modules improve navigation.
3. Implementation Quality: 6.5/10 — the full synthetic vertical slice is live.
4. Architecture & Design: 6.0/10 — one canonical runtime exists, but persistence and real mode remain.
5. Performance & Optimization: 5.5/10 — bounded synthetic execution is fast; no durable cache yet.
6. Security: 6.0/10 — stable errors, request IDs, strict CORS and non-root image are present.
7. Testing Quality: 5.0/10 — smoke verification passes, but canonical test suites are still being migrated.
8. Documentation: 6.0/10 — README and execution plan exist; API examples are still expanding.
9. Scalability: 4.5/10 — in-memory repository and polling are MVP-only.
10. DevOps Practices: 5.5/10 — package metadata and Docker hardening exist; CI and migrations remain.
11. User Experience: 6.5/10 — stable run/snapshot/timeline/trace/counterfactual contract now supports frontend integration.

Overall: 5.9/10
Maturity: Prototype with a working synthetic vertical slice

## Top strengths

- One canonical `app.main:app` is now available.
- The frontend can integrate against stable `/v1` routes.
- Scientific values are generated by the backend and carried in the snapshot.
- Event order, replay, and terminal completion are represented explicitly.

## Biggest weaknesses

- Persistence is not durable.
- Real-data adapter is not yet connected.
- Canonical tests and contract export still need to be added.

## Critical fixes for the next phase

- Add complete contract tests and generated OpenAPI artifact.
- Migrate scientific tests into `unit`, `property`, and `integration` groups.
- Add a PostgreSQL/artifact repository behind the repository protocol.
- Migrate the prepared 1000 Genomes region adapter.

## Five next improvements

1. Freeze the OpenAPI schema and frontend fixtures.
2. Add property tests for segment and Mendelian invariants.
3. Add PostgreSQL persistence and migration scripts.
4. Add a verified real-trio manifest and disclosure path.
5. Add CI, coverage and Docker build checks.
